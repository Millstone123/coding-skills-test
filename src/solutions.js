// Coding Skills Test - Your Implementations
//
// Implement each function to pass all test cases.
// These will be validated against live solutions from m100.cloud/code-answers

export function sumArray(arr) {
  // TODO: Return the sum of all numbers in the array
  // Example: sumArray([1, 2, 3]) should return 6
  // Edge cases: empty array should return 0
}

export function filterEven(arr) {
  // TODO: Return a new array containing only even numbers
  // Example: filterEven([1, 2, 3, 4]) should return [2, 4]
  // Important: Preserve the original order
}

export function reverseString(str) {
  // TODO: Return the string reversed
  // Example: reverseString("hello") should return "olleh"
  // Edge cases: empty string and special characters
}

export function findMax(arr) {
  // TODO: Return the largest number in the array
  // Example: findMax([3, 1, 4, 1, 5]) should return 5
  // Important: Throw an error if the array is empty
}

export function removeDuplicates(arr) {
  // TODO: Return a new array with duplicate values removed
  // Example: removeDuplicates([1, 2, 2, 3, 3, 3]) should return [1, 2, 3]
  // Important: Preserve the order of first occurrence
}
